.logo
  color white !important
  text-decoration none !important
  display flex
  align-items center
  padding 0 15px 2px 40px
  background inline-image('logo.svg') no-repeat 0 50%/30px auto
  font-size 19px
  font-weight 500
  white-space nowrap